export ZPROFILE_SOURCED=1
typeset -U path
path=(~/bin ~/.local/bin /usr/local/bin $path)
stty -ixon  # disable tty flow control, ie. CTRL-S, CTRL-Q



if [ -d $HOME/.krew ]; then
    path=(~/.krew/bin $path)
fi
if [ -d $HOME/.local/kotlinc/ ]; then
    path=(~/.local/kotlinc/bin $path)
fi
